<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="edu.url.salle.eric.macia.bubblefy">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/bubblefy_logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/bubblefy_logo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".controller.activity.SearchActivity"></activity>
        <activity android:name=".controller.activity.ListActivity" />
        <activity
            android:name=".controller.activity.SplashActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".controller.activity.RecoveryEmail" />
        <activity android:name=".controller.activity.NewPassword" />
        <activity android:name=".controller.activity.RecoverKey" />
        <activity android:name=".controller.activity.RegisterActivity" />
        <activity android:name=".controller.activity.LoginActivity" />
        <activity android:name=".controller.activity.MainActivity" />
    </application>

</manifest>